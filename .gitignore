# Local overrides — never commit tokens
assets/bug-config.json
.dev.vars
.netlify

# Local PVWatts log. Resume cells for an unfinished town live in
# scripts/pvwatts-progress/ and are committed so the hourly job can continue.
scripts/.pvwatts-cache/
scripts/fetch-quebec-towns.log
